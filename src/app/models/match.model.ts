export class Match {
    _id: string;

    constructor(_id: string) {
        this._id = _id;
    }
}

export class Matches {
    _id: string;

    constructor(_id: string) {
        this._id = _id;
    }
}